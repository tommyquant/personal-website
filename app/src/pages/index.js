import React from 'react';
import PropTypes from 'prop-types';

const Home = ({
    data
}) => {
    return (
        <span>Hello World!</span>
    );
};

Home.propTypes = {
    data: PropTypes.any
};

export default Home;
